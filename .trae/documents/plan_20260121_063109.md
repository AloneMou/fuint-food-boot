I will refactor `OpenOrderController` to delegate all business logic to `OpenApiOrderService`, ensuring strict typing (no `Map` returns) and cleaner controller code.

### 1. Define Transfer Objects (VOs/DTOs)
*   **Create `OrderDetailRespVO`**: To replace the `Map` return in `getOrderDetail`. It will contain the order information and queue details.
*   **Reuse/Update Existing VOs**: ensure `OrderPreCreateRespVO` and others are available for the service layer.

### 2. Refactor `OpenApiOrderService` Interface
I will update the interface to include all necessary methods currently handled by the controller or `OrderService`:
*   `preCreateOrder`: Update return type from `Map` to `OrderPreCreateRespVO`.
*   `createOrder`: New method returning `UserOrderDto`.
*   `cancelOrder`: New method.
*   `payOrder`: New method.
*   `refundOrder`: New method.
*   `getOrderDetail`: New method returning `OrderDetailRespVO`.
*   `getOrderList`: New method returning `PageResult<UserOrderDto>`.
*   `evaluateOrder`: New method.
*   `getEvaluations`: New method.
*   `markOrderReady`: New method.

### 3. Implement `OpenApiOrderServiceImpl`
I will move the business logic from `OpenOrderController` into this service implementation:
*   **Migration**: Move validation logic (User existence, Store status, Goods availability) from Controller to Service.
*   **Optimization**: Rewrite `preCreateOrder` and `calculateCartGoods` to return strong types.
*   **Integration**: Call `OrderService`, `MemberService`, `SettingService`, etc., internally within `OpenApiOrderServiceImpl`.

### 4. Refactor `OpenOrderController`
*   **Slim Down**: Remove all direct dependencies on `OrderService`, `MemberService`, `CartService`, etc.
*   **Delegate**: Replace complex logic with single calls to `OpenApiOrderService`.
*   **Cleanup**: Remove unused imports and private helper methods that are moved to the service.
